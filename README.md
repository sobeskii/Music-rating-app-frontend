## Music rating app frontend
* Navigate to ``frontend`` directory and run ``npm install`` command
* Edit ```.env``` file (values will differ depending on environment)
* Run ```npm run serve``` command
